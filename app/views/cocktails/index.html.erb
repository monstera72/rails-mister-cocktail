<div class="container">
  <div class="h1">List of cocktails</div>

<ul>
    <% @cocktails.each do |cocktail| %>
      <li><%= link_to cocktail.name, cocktail_path(cocktail) %></li><% end %>
  </ul>

  <%= link_to "Add a cocktail", new_cocktail_path, class: 'btn btn-primary'  %>


</div>
