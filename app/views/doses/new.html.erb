<div class="container">
<h1>new dose</h1>

  <%= simple_form_for [@cocktail, @dose] do |f| %>
    <%= f.input :description %>
    <%= f.input :ingredient_id, collection: Ingredient.all  %>

    <%= f.submit "Save my dose", class: 'btn btn-primary' %>
  <% end %>



</div>
